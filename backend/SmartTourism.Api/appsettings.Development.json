{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  },

  "_comment": "Development-only values. This file is fine to commit because the SQL Server password below is a throwaway local Docker password (see docker-compose.yml), not a real secret. Still gitignored by default — see .gitignore.",
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost,1433;Database=SmartTourismDb;User Id=sa;Password=Dev_Local_Only_2026!;TrustServerCertificate=True;MultipleActiveResultSets=true"
  },

  "Jwt": {
    "Secret": "DEV_ONLY_super_secret_signing_key_min_32_chars_change_me",
    "Issuer": "SmartTourism.Api",
    "Audience": "SmartTourism.Client",
    "AccessTokenExpiryMinutes": 60,
    "RefreshTokenExpiryDays": 7
  },

  "Cors": {
    "AllowedOrigins": [ "http://localhost:5173", "http://127.0.0.1:5173" ]
  },

  "_comment_GoogleAuth": "Set your own OAuth Client ID here for local dev, or export GOOGLE_CLIENT_ID instead (either works; the env var wins if both are set).",
  "GoogleAuth": {
    "ClientId": ""
  },

  "SeedAdmin": {
    "Email": "admin@yatra.com",
    "FullName": "Yatra Platform Admin"
  }
}
