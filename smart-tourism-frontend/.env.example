# Copy to .env and adjust if your backend runs on a different port/host.
VITE_API_URL=http://localhost:5000/api

# Google OAuth 2.0 Client ID (public, not a secret). Must match the
# backend's GOOGLE_CLIENT_ID exactly.
VITE_GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID_HERE
